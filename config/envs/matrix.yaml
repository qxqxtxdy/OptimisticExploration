# --- config for one step matrix game ---

env: "matrix_game"

env_args:
    steps: 1
    seed: null

save_continue: True
batch_size: 32
test_greedy: True
test_nepisode: 4
test_interval: 10
log_interval: 10
runner_log_interval: 10
learner_log_interval: 10
t_max: 1000
